{
  "name": "@nodes/save-as",
  "main": "saveAsNode.ts",
  "types": "saveAsNode.ts",
  "scripts": {
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist"
  },
  "dependencies": {
    "@shared/node": "workspace:@shared/node-v0.8.0@*",
    "@shared/port": "workspace:@shared/port-v0.6.0@*",
    "@shared/save-as-v1.0.0": "workspace:*"
  },
  "devDependencies": {
    "@shared/typescript-config": "workspace:*"
  }
}
