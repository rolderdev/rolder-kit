---
id: node-handlers/markdown
tasks:
  build:
    command: rspack build --mode=production
    inputs:
      - src/**/*
      - /packages/components/ui/markdown/**/*
      - /packages/internal/**/*
  dev:
    command: rspack build --mode=development --watch
    inputs:
      - src/**/*
      - /packages/components/ui/markdown/**/*
      - /packages/internal/**/*
    local: true
