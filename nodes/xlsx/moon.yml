---
id: node-handlers/xlsx
tasks:
  build:
    command: rspack build --mode=production
    inputs:
      - src/**/*
      - /packages/components/xlsx/**/*
      - /packages/internal/**/*
  dev:
    command: rspack build --mode=development --watch
    inputs:
      - src/**/*
      - /packages/components/xlsx/**/*
      - /packages/internal/**/*
    local: true
