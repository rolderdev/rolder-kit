{
  "name": "rolder-kit",
  "version": "v0.29.0",
  "private": true,  
  "scripts": {
    "build": "turbo run build",
    "noodl": "turbo run build --force --filter=!docs",
    "dev": "turbo run dev --concurrency 50",
    "clean": "turbo run clean & rm -rf node_modules"
  },
  "devDependencies": {    
    "turbo": "1.11.3"
  },
  "packageManager": "pnpm@8.14.1"
}